using System;

class LeastMajorityMultiple
{
    static void Main()
    {
        int count = 0;
        int[] myArray = new int[5];
        for (int i = 0; i < 5; i++)
        {
            myArray[i] = int.Parse(Console.ReadLine());
        }
        Array.Sort(myArray);
        while (true)
        {

            count++;
            if...
        }
    }
}